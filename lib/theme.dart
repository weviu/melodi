import 'package:flutter/material.dart';

// Lily colors (from the SVG logo)
const Color kLilyDark = Color(0xFF0F2F6A); // filled buttons, active icons, progress bars
const Color kLilyLight = Color(0xFF153A80); // hover overlays, gradients, text links

// Backgrounds
const Color kBgDark = Color(0xFF121212); // main scaffold background
const Color kBgSurface = Color(0xFF181818); // cards, app bar, nav bar
const Color kHoverBg = Color(0xFF282828); // hover state background

// Text
const Color kTextPrimary = Color(0xFFFFFFFF);
const Color kTextSecondary = Color(0xFFB3B3B3);
